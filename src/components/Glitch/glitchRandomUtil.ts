export const randRect = () => {
  const rand = Math.floor(Math.random() * 200) + 1;
  return rand;
}

export const randPick = () => {
  const rand = Math.floor(Math.random() * 20) + 1;
  return rand;
}